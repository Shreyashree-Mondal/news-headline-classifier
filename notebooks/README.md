Put `Mondal_HW2.ipynb` here. After re-running it, paste `export_artifacts_cell.py`
as the last cell, run it, and move the generated `models/` folder to the repo root.
The dataset (`News_Category_Dataset_v3.json`) is not committed; download it from Kaggle.
